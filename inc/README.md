# Auxiliary files to auto-import

In this directory you'll place all the auxiliary php files that you want to 
automatically import into you `functions.php` file.

Files are imported in alphabetic order.